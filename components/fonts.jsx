import { Global } from "@emotion/react";

const Fonts = () => (
  <Global
    styles={`
      // aqui se pueden importar los estilos globales (fuentes de google, etc)
    `}
  />
);

export default Fonts;
